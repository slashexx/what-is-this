import React from "react";

export const metadata = {
    title: "Reset Password | Bhaw Bhaw Admin Panel",
    description: "Reset your password for the Bhaw Bhaw Admin Panel. Ensure secure access to platform management tools and maintain uninterrupted oversight of operations.",
    keywords: "admin forget password, password reset, admin panel access"
  };
    
  const Layout = ({ children }) => {
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    