import React from "react";

export const metadata = {
    title: "Admin Login | Juristo",
    description: "Log in to the Juristo Admin Panel to manage platform operations effectively. Access tools for overseeing users and financial transactions. Ensure seamless functionality and exceptional service delivery.",
    keywords: "admin login, Juristo admin access, platform management"
    };

const Layout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default Layout;
