import React from "react";

export const metadata = {
    title: "Manage Categories | Bhaw Bhaw Admin Panel",
    description: "Organize and manage categories for products and services on the Bhaw Bhaw platform. Ensure intuitive navigation and a user-friendly experience by maintaining a well-structured hierarchy of categories.",
    keywords: "admin categories, platform organization, category management, user experience"
  };    
    
  const Layout = ({ children }) => {
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    