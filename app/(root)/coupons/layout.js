import React from "react";

export const metadata = {
    title: "Manage Coupons | Bhaw Bhaw Admin Panel",
    description: "Create, manage, and track promotional coupons to boost user engagement and vendor sales. Customize discounts for products and services while monitoring coupon performance. Use effective promotions to drive traffic and enhance the platform’s appeal.",
    keywords: "admin coupons, promotional tools, discount management, sales boost"
  };
  
    
  const Layout = ({ children }) => {
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    