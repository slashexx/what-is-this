import React from "react";

export const metadata = {
    title: "Juristo | Manage users and services",
    description: "Monitor key metrics and gain insights into juristo's operations. From sales performance to user activity, the dashboard equips admins with powerful tools for seamless management and analysis.",
    keywords: "admin home, platform management, juristo tools, operational insights"
  };
  
  const Layout = ({ children }) => {
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    