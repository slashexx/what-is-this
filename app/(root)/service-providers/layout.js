import React from "react";

export const metadata = {
    title: "Manage Services Providers | Bhaw Bhaw Admin Panel",
    description: "Oversee all service providers on the Bhaw Bhaw platform. Manage registrations, monitor service quality, and ensure providers adhere to platform standards. Resolve provider queries, track performance, and maintain a network of trusted pet care experts.",
    keywords: "admin service providers, provider management, service quality monitoring"
  };  

const Layout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default Layout;
