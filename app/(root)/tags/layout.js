import React from "react";

export const metadata = {
    title: "Manage Tags | Bhaw Bhaw Admin Panel",
    description: "Create and manage tags to categorize products, services, and content effectively on the Bhaw Bhaw platform. Use tags to improve searchability and organization, ensuring users can easily find what they need. Keep the platform user-friendly with a robust tagging system.",
    keywords: "admin tags, tagging system, content categorization, platform organization"
  };

const Layout = ({ children }) => {
  return (
    <div>
      {children}
    </div>
  );
};

export default Layout;
