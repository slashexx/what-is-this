import React from "react";

export const metadata = {
    title: "Vendor Management | Bhaw Bhaw Admin Panel",
    description: "Oversee vendor accounts, products, and performance on the Bhaw Bhaw Admin Panel. Ensure quality services and products from trusted partners.",
    keywords: "admin vendor helpdesk, vendor support, query resolution, vendor assistance"
  };
          
  const Layout = ({ children }) => {
    return (
      <div>
        {children}
      </div>
    );
  };
  
  export default Layout;
    